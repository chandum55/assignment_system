# GradeFlow Deployment Configuration

# 1. Prevent Directory Listing
Options -Indexes

# 2. Prevent Access to sensitive files
<FilesMatch "^\.|\.sql$|config\.php$">
    Order allow,deny
    Deny from all
</FilesMatch>

# 3. Security Headers
<IfModule mod_headers.c>
    Header set X-XSS-Protection "1; mode=block"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set X-Content-Type-Options "nosniff"
    Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# 4. Handle Uploads Security
# Prevent execution of scripts in the uploads folder
# This rule is already inside the project root, so we check the uploads path
<IfModule mod_rewrite.c>
    RewriteRule ^uploads/.*\.php$ - [F,L]
</IfModule>

# Alternative if mod_rewrite isn't used (Direct match)
<FilesMatch "^uploads/.*\.(php|phtml|php3|php4|php5|phps)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>
